---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Home — Astro Vanilla CSS">
  <section class="stack">
    <article class="prose">
      <h1>Design Tokens in Vanilla CSS</h1>
      <p>This starter demonstrates tokens, layers, small utilities, and semantic components without Tailwind or preprocessors.</p>
      <pre><code>:root { --brand-500: 231 96% 59%; }</code></pre>
    </article>

    <div class="c-card">
      <h2>Featured</h2>
      <p>A simple card component styled via the <code>.c-card</code> class.</p>
      <div class="mt-4">
        <button class="c-btn c-btn--primary">Primary</button>
        <button class="c-btn c-btn--ghost" style="margin-left: var(--space-2)">Ghost</button>
      </div>
    </div>

    <div class="c-card-grid">
      <div class="c-card">Card A</div>
      <div class="c-card">Card B</div>
    </div>
  </section>
</BaseLayout>